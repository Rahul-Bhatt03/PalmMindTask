# API base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:4000

# Socket.IO server URL (defaults to API base URL when empty)
VITE_SOCKET_URL=

# App environment label: development | staging | production
VITE_APP_ENV=development
